@model Mark1.Models.ViewModels.ExpenseFormViewModel
@{
    ViewData["Title"] = Localizer["Expenses_EditTitle"];
}

<h1 class="page-title">@Localizer["Expenses_EditTitle"]</h1>
<partial name="_Form" />

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
